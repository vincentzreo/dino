async function hello(req) {
    return {
        status: 200,
        headers: {
            "content-type": "application/json",
        },
        body: JSON.stringify(req),
    }
}

export { hello };
